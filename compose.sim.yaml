#xhost +local:

services:
  ros2_gazebo:
    build: .
    privileged: true
    entrypoint: "terminator"
    network_mode: host  
    ipc: "host"
    pid: "host"
    environment:
      - IS_SIM=True
      - DISPLAY
    volumes:
      - ./data:/root/data
      - /dev/input:/dev/input
